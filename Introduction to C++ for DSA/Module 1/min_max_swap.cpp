#include <bits/stdc++.h>
#include <algorithm>
using namespace std;

// void my_swap(int *a, int *b)
// {
//    int  tmp = *a;
//     *a = *b;
//     *b = tmp;
// }

int main()
{

    int a,b;
    cin >> a >> b;
    swap(a, b);

    cout << a << " " << b << endl;
  


    // int a, b,c,d;
    // cin >> a >> b >> c >> d;

    // int mn = min({a,b,c,d});
    // int mx = max({a,b,c,d});

    // cout << mx << endl;
    // cout << mn <<endl;

    return 0; 
}